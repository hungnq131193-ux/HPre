# HPre 1.0.20

## Thay doi

- VOD va livestream uu tien HLS/DASH adaptive; progressive/merged A/V giu fallback khi manifest khong co. Live gate cho thay manifest phat on dinh hon direct stream tren upstream hien tai.
- Media3 refresh stream toi da mot lan cho loi delivery tam thoi (401, 403, 404, 5xx, connection failure, timeout), giu vi tri, y dinh phat va toc do.
- Recovery uu tien source type chua thu sau refresh; khong loop vo han va khong auto-retry cac restriction da map.
- Telemetry mo video trong release chi ghi milestone, thoi gian, generation va error/source category da map; khong ghi identity hay du lieu nhay cam.

## Xac minh

- `testDebugUnitTest`, `lintDebug`, `assembleDebug` va `assembleDebugAndroidTest`: PASS.
- `clean assembleRelease lintVitalRelease`: PASS.
- APK: `HPre-v1.0.20-release.apk` (3,684,986 bytes), SHA-256 `AFBC65E81517FCF21037FDE3492891BFB6905A7E560AEA83ABA029C7AC9422D2`.
- APK Signature Scheme v2: hop le. Certificate SHA-256 `40D629544B6C12014AD0A03BEDC2AFFE81E6DF8EF88F693F76365A6E95D49D47`.
- Static upgrade 1.0.19 (20) sang 1.0.20 (21): PASS. Certificate SHA-256 trung baseline.
- Install-over upgrade: PASS tren `emulator-5554` API 35, gom `adb install -r` va marker DataStore.
- Upstream smoke: PASS tren emulator API 35 voi query duoc phe duyet; query va candidate identifiers khong luu trong artifact.
- Live Media3 playback gate: PASS tren emulator API 35, xac nhan surface, ready, first frame, advance va seek cho manifest runtime source.
- Full connected suite: BLOCKED boi test navigation baseline `NavigationFlowTest.system_back_preserves_pop_back_stack_semantics_without_calling_minimize_helper`; cung test nay fail tren `main` 1.0.19 va khong nam trong thay doi 1.0.20.

## Phat hanh

- Release asset: `HPre-v1.0.20-release.apk`.
